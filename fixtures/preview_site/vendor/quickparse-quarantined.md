# quickparse — quarantined

Nobody paid for an analysis of 0.4.1, so nobody here knows what it does. The
build ships with this dependency switched off: tickets that need parsing are
queued for a human instead of being handled automatically.

Safe, and slower. Buying the analysis is what avoids this.
